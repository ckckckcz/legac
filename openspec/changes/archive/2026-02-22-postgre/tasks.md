# Tasks: Implement PostgreSQL Schema

## Database Initialization

- [x] Create `database/init.sql` with basic setup (extensions).
- [x] Create `database/tables.sql` with table definitions for `users`, `repositories`, `documentations`, and `documentation_pages`.
- [x] Add foreign key constraints and indices.

## Verification

- [x] Verify that the `database` folder contains the new SQL files.
- [x] Ensure `documentations` table supports the `legacyver-docs` structure.
